name: Lint
on:
  push:
    branches:
    - master
jobs:
  main:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@master
    - name: Yarn Install
      run: yarn
    - name: Lint
      run: yarn lint
