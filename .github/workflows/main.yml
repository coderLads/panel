name: Lint

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      run: actions/checkout@master
    - name: Install
      run: yarn
    - name: Lint
      run: yarn lint
